function [  ] = PreprocessRecording( recordingName, setting )
% Preprocessing the recording includes voice activity detecttion (cut into
% utteranc), segmentation (cut into processing unit), extract acoustic
% features. 

%% Read audio
[ recording, sampleRate ] = audioread( filename );

%% Voice activity detection
VoiceActivityDetection( recording, sampleRate , setting );

%% 

end

